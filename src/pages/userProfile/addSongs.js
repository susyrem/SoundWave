document.addEventListener("DOMContentLoaded", function() {
    const buttons = document.querySelectorAll('#connect-spotify');
    buttons.forEach(button => {
        button.addEventListener('click', function() {
            // Código para agregar más canciones
        });
    });
});
